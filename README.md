# Персональний Асистент
Це головний клас-контролер (`PersonalAssistant`) для запуску консольного (CLI) бота-асистента. Він керує життєвим циклом програми: завантажує дані, обробляє команди користувача, викликає відповідні обробники (handlers) та автоматично зберігає зміни.

## 🚀 Основні Можливості
* **Автоматичне збереження:** Дані адресної книги (`abook.pkl`) та книги нотаток (`nbook.pkl`) автоматично зберігаються в домашній директорії користувача (`~/`) після виконання будь-якої команди, що змінює дані (тобто, будь-якої, що не починається з "get").
* **Автоматичне завантаження:** Дані завантажуються з файлів `.pkl` під час кожного запуску. Якщо файли не знайдено, створюються нові, порожні екземпляри `AddressBook` та `NoteBook`.
* **"Розумні" пропозиції:** Якщо користувач вводить невідому команду, система використовує `difflib`, щоб знайти схожі команди (зі схожістю >50%) і пропонує виконати правильний варіант.
* **Підтвердження пропозицій:** Користувач повинен підтвердити запропоновану команду (ввівши `y`), перш ніж вона буде виконана.
* **Розширювана Архітектура:** Логіка чітко розділена. `PersonalAssistant` діє як "маршрутизатор" (роутер), який передає команди спеціалізованим обробникам `PersonalAssistantAddressBookHandler` і `PersonalAssistantNoteBookHandler`.

## 🗂️ Очікувана Структура Проекту
Для коректної роботи, цей проект очікує, що у вашому проекті існують наступні файли та класи:
## ⚙️ Встановлення та Запуск

1.  Переконайтеся, що всі вищезгадані файли (`__init__.py`, `address_book.py`, `note_book.py`, `general.py`, `personal_assistant_address_book_handler.py`, `personal_assistant_note_book_handler.py` та `personal_assistant.py`) знаходяться у вашій директорії в паппці src.
2.  Створіть головний файл `main.py`.
3.  У файлі `main.py` імпортуйте та запустіть асистента:

    ```python
    # main.py
    import src.personal_assistant

    if __name__ == "__main__":
        src.personal_assistant.run()
    ```

4.  Запустіть програму з вашого терміналу:

    ```bash
    python main.py
    ```

## 📖 Список Команд

### 📌 Загальні Команди
| Команда | Опис |
| :--- | :--- |
| `version` | Поточна версія асистента. |
| `help` | Допомога. Список команд. |
| `hello` | Вітається з користувачем. |
| `clear` | Очищує екран консолі. |
| `close`, `exit`, `bye`, `bye-bye` | Завершує роботу асистента та зберігає дані. |

### 📖 Команди Адресної Книги
| Команда | Опис |
| :--- | :--- |
| `add` | Додає новий контакт. Не обовязкові параметри [phone, email, birthday, address] |
| `add-phone` | Додає номер телефону до існуючого контакту. |
| `add-email` | Додає email до існуючого контакту, ао замінює існуючий. |
| `add-birthday` | Додає/змінює день народження контакту. |
| `add-address` | Додає/змінює адресу контакту. |
| `get-birthday` | Показує день народження контакту. |
| `get-upcoming-birthdays`| Показує список днів народжень на найближчі дні. |
| `get-phone` | Показує телефони вказаного контакту. |
| `get-all` | Показує всі контакти в адресній книзі. |
| `get-info` | Показує повну інформацію про контакт. |
| `change-phone` | Змінює номер телефону контакту. |
| `change-email` | Змінює email контакту. |
| `change-birthday`| Змінює день народження контакту. |
| `change-address` | Змінює адресу контакту. |

### 📓 Команди Книги Нотаток